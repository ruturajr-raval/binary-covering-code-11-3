[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "binary-covering-code-11-3"
version = "0.1.0"
description = "Verification and exact-search tools for K_2(11,3)"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
  {name = "Ruturaj R Raval"}
]

[tool.setuptools.packages.find]
where = ["src"]
